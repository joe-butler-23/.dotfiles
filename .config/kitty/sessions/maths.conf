# Kitty session for Maths Project
# This file defines the terminal layout for maths work

# Create windows
new_window "Maths Terminal"
launch --type=tab --cwd=~/projects/maths
send_text "cd ~/projects/maths && clear"
send_text "echo 'Maths Project Terminal Ready'"

new_window "Python REPL"
launch --type=tab --cwd=~/projects/maths
send_text "python3"
send_text "import numpy as np"
send_text "import matplotlib.pyplot as plt"

new_window "Git Status"
launch --type=tab --cwd=~/projects/maths
send_text "git status"
